# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is newly initialized and currently empty. Update this file as the project is built out.

## Getting Started

Add project setup instructions here once the tech stack is decided (e.g., `npm install`, `pip install -r requirements.txt`).

## Common Commands

Add build, lint, test, and run commands here once the project structure is established.

## Architecture

Add high-level architecture notes here as the project grows — entry points, key modules, data flow, etc.
