import type { PracticeSheetSpec } from "../practice-pdf-builder";

export const specificPractice: PracticeSheetSpec[] = [

  // ── Java / JVM ──────────────────────────────────────────────────────────
  {
    name: "Spring Boot",
    subtitle: "10 conceptual Q&A covering auto-config, JPA, security, and testing",
    accentHex: "#6DB33F",
    pairs: [
      { q: "What is Spring Boot and how does it differ from the Spring Framework?",
        a: "Spring Boot is a Spring module that provides auto-configuration, embedded servers, and starter POMs. Unlike plain Spring, it needs zero XML and runs as a self-contained JAR, removing the need to deploy to a separate app server." },
      { q: "How does Spring Boot auto-configuration work under the hood?",
        a: "@EnableAutoConfiguration loads candidate classes from AutoConfiguration.imports. Each class uses @ConditionalOnClass or @ConditionalOnMissingBean to activate only when specific dependencies or beans are present on the classpath." },
      { q: "What does @SpringBootApplication combine and why is each part needed?",
        a: "It merges @Configuration (declares beans), @EnableAutoConfiguration (activates Boot's auto-config machinery), and @ComponentScan (recursively discovers @Component, @Service, @Repository in the package tree)." },
      { q: "How does Spring Boot externalize and prioritize configuration?",
        a: "Boot loads sources in ascending-priority order: application.yml, profile files, OS environment variables, and CLI args each override the previous. @ConfigurationProperties binds a prefix to a typed POJO with IDE-validation support." },
      { q: "What is Spring Boot Actuator and what does it expose?",
        a: "Actuator adds management HTTP endpoints: /actuator/health (liveness/readiness), /actuator/metrics (counters, timers), /actuator/loggers (change log levels at runtime), and /actuator/env. Each endpoint is securable and toggleable." },
      { q: "How do you configure a relational database in Spring Boot?",
        a: "Add spring-boot-starter-data-jpa and a JDBC driver. Set spring.datasource.url, username, and password in application.properties. Boot auto-wires a DataSource, EntityManagerFactory, and JpaTransactionManager without any explicit @Bean definitions." },
      { q: "How do you replace the default embedded Tomcat with Jetty?",
        a: "Exclude spring-boot-starter-tomcat from the web starter, then add spring-boot-starter-jetty. Auto-configuration detects Jetty on the classpath and selects it. Undertow is a third option available the same way." },
      { q: "How do you implement global REST exception handling in Spring Boot?",
        a: "@RestControllerAdvice marks a class as a cross-cutting error handler. Methods annotated @ExceptionHandler(SomeEx.class) intercept matched exceptions and return a ResponseEntity with a structured error body and HTTP status code." },
      { q: "What are Spring Boot profiles and how do you activate one?",
        a: "Profiles separate environment configuration. application-prod.yml is only loaded when the prod profile is active. Activate via spring.profiles.active=prod in properties, the SPRING_PROFILES_ACTIVE env var, or --spring.profiles.active=prod CLI arg." },
      { q: "How do you write a Spring Boot integration test?",
        a: "@SpringBootTest starts a full application context. Add @AutoConfigureMockMvc to inject a MockMvc for HTTP-level assertions without a real server. Use @MockBean to replace infrastructure beans (e.g., database, email) with Mockito doubles." },
    ],
  },

  {
    name: "Spring Framework",
    subtitle: "10 conceptual Q&A on IoC, AOP, MVC, and transaction management",
    accentHex: "#6DB33F",
    pairs: [
      { q: "What is Inversion of Control (IoC) and how does Spring implement it?",
        a: "IoC means the framework creates and wires objects instead of the code. Spring's IoC container reads bean definitions from @Configuration classes or XML, instantiates objects, and injects dependencies via constructors or setters." },
      { q: "Describe the Spring Bean lifecycle from creation to destruction.",
        a: "The container instantiates the bean, injects dependencies, calls @PostConstruct (or afterPropertiesSet), then makes it available. On context close, @PreDestroy (or destroy()) is called before the bean is discarded." },
      { q: "What is the difference between BeanFactory and ApplicationContext?",
        a: "BeanFactory is the minimal IoC container with lazy initialization. ApplicationContext extends it with eager init, event publishing, i18n, and AOP support. ApplicationContext is used in virtually all production Spring apps." },
      { q: "Explain the four main Spring bean scopes.",
        a: "Singleton (one instance per container, the default), Prototype (new instance per injection), Request (one per HTTP request), and Session (one per HTTP session). Request and Session require a web-aware context." },
      { q: "How does Spring AOP work and what are its key concepts?",
        a: "Spring AOP creates JDK dynamic proxies (or CGLIB for classes). A Pointcut selects joinpoints (method calls). Advice (@Before, @After, @Around) runs at those joinpoints. An Aspect bundles pointcuts and advice together." },
      { q: "What is @Transactional and what propagation levels matter most?",
        a: "REQUIRED (default) joins an existing transaction or creates one. REQUIRES_NEW always creates a new transaction, suspending the current one. SUPPORTS runs in a transaction if one exists but doesn't require it." },
      { q: "Walk through the Spring MVC DispatcherServlet request lifecycle.",
        a: "DispatcherServlet consults HandlerMapping to find the controller method, then HandlerAdapter to invoke it. ViewResolver converts the logical view name to a View. The View renders and DispatcherServlet writes the response." },
      { q: "How does @Autowired resolve injection ambiguity?",
        a: "@Autowired first matches by type. If multiple beans qualify, @Primary marks the preferred one. @Qualifier('beanName') selects a specific bean by name. Constructor injection is preferred because it makes dependencies explicit." },
      { q: "How does Spring's ApplicationEvent system work?",
        a: "Beans publish events via ApplicationEventPublisher.publishEvent(event). Beans annotated @EventListener(MyEvent.class) or implementing ApplicationListener<MyEvent> receive them. Events can be async with @Async on the listener." },
      { q: "How does Spring's cache abstraction work?",
        a: "@EnableCaching activates a proxy that intercepts @Cacheable methods. On the first call the result is stored in the named cache; subsequent calls return the cached value without executing the method body. @CacheEvict removes entries." },
    ],
  },

  {
    name: "Spring Security",
    subtitle: "10 Q&A on filter chain, JWT, OAuth2, CSRF, and method security",
    accentHex: "#6DB33F",
    pairs: [
      { q: "What is the Spring Security filter chain and how does it process requests?",
        a: "Spring Security registers a chain of servlet filters via DelegatingFilterProxy. Each filter handles a concern: SecurityContextPersistenceFilter restores session state, UsernamePasswordAuthenticationFilter processes form logins, and ExceptionTranslationFilter maps exceptions to HTTP responses." },
      { q: "What is the difference between Authentication and Authorization?",
        a: "Authentication verifies who the user is (username/password, JWT, OAuth2 token). Authorization decides what they can do (can this principal access this resource?). Spring Security does auth first, then authz on the resolved principal." },
      { q: "How do you implement stateless JWT authentication in Spring Security?",
        a: "Disable sessions (SessionCreationPolicy.STATELESS), add a custom OncePerRequestFilter that parses the JWT, builds a UsernamePasswordAuthenticationToken, and stores it in SecurityContextHolder. Protect routes with .authorizeHttpRequests()." },
      { q: "What does UserDetailsService do in Spring Security?",
        a: "UserDetailsService.loadUserByUsername(username) returns a UserDetails object containing the username, hashed password, and granted authorities. DaoAuthenticationProvider calls it during form-login to authenticate against a database user." },
      { q: "What are hasRole(), hasAuthority(), and isAuthenticated() security expressions?",
        a: "hasRole('ADMIN') checks for ROLE_ADMIN in GrantedAuthorities (Spring auto-prefixes). hasAuthority('SCOPE_read') checks the exact authority. isAuthenticated() is true for any non-anonymous principal. Use them in @PreAuthorize or requestMatchers." },
      { q: "How does method-level security work in Spring Security?",
        a: "Add @EnableMethodSecurity to a config class. Annotate service methods with @PreAuthorize('hasRole(\"ADMIN\")') or @PostAuthorize. Spring wraps the bean in a proxy that evaluates the SpEL expression before or after the method runs." },
      { q: "What is an OAuth2 Resource Server and how do you configure one?",
        a: "A Resource Server validates Bearer tokens on incoming requests. Configure it with .oauth2ResourceServer(oauth2 -> oauth2.jwt(...)) and spring.security.oauth2.resourceserver.jwt.issuer-uri pointing to your authorization server for JWK key discovery." },
      { q: "How do you configure CORS in a Spring Security application?",
        a: "Define a CorsConfigurationSource bean or use @CrossOrigin, then call .cors(cors -> cors.configurationSource(source)) in the security filter chain BEFORE other filters. Without this, Spring Security blocks CORS preflight OPTIONS requests." },
      { q: "What is CSRF and when can you safely disable it?",
        a: "CSRF forges state-changing requests using a victim's authenticated session. Disable it only for stateless REST APIs that authenticate via headers (JWT, API keys) rather than cookies — browsers cannot set custom headers from attacker pages." },
      { q: "How do you test Spring Security with @WithMockUser?",
        a: "@WithMockUser('user') populates the SecurityContext with a mock principal for the duration of the test. @WithSecurityContext lets you build a fully custom context. Use MockMvc with springSecurity() configurer to apply filter chain logic." },
    ],
  },

  {
    name: "Spring Cloud",
    subtitle: "10 Q&A on service discovery, gateway, circuit breaking, and tracing",
    accentHex: "#6DB33F",
    pairs: [
      { q: "What problems does Spring Cloud solve in a microservices architecture?",
        a: "Spring Cloud provides turnkey solutions for service discovery (Eureka), distributed configuration (Config Server), API gateway (Gateway), circuit breaking (Resilience4j), and distributed tracing (Micrometer + Zipkin) — the core operational concerns of microservices." },
      { q: "How does Eureka service discovery work in Spring Cloud?",
        a: "Services annotated @EnableEurekaClient register with the Eureka server on startup (heartbeat every 30 s). Clients use DiscoveryClient or a load-balanced RestTemplate/WebClient to resolve service names to running instances by querying Eureka." },
      { q: "What is Spring Cloud Gateway and how do you define a route?",
        a: "Gateway is a reactive API gateway built on Spring WebFlux. Routes are defined by predicates (Path=/api/**) and filters (AddRequestHeader, CircuitBreaker, RateLimiter). Requests matching a predicate are forwarded to the configured URI." },
      { q: "How does Spring Cloud Config Server manage distributed configuration?",
        a: "Config Server serves property files stored in a Git repo (or file system). Client applications fetch their config from Config Server at startup using spring.application.name and active profiles. Refresh without restart is possible via /actuator/refresh + @RefreshScope." },
      { q: "How does Resilience4j circuit breaking work in Spring Cloud?",
        a: "A circuit breaker wraps a service call and counts failures. When failures exceed a threshold the circuit opens, and calls immediately return a fallback without hitting the downstream service. After a wait duration the circuit enters HALF_OPEN to test recovery." },
      { q: "What is distributed tracing in Spring Cloud and how does Micrometer help?",
        a: "Micrometer Tracing injects a trace ID and span ID into every request, propagating them via HTTP headers (B3 or W3C). Spring Cloud integrates with Zipkin or Grafana Tempo. All spans for one user request share the same trace ID for correlation." },
      { q: "How does Spring Cloud LoadBalancer perform client-side load balancing?",
        a: "A @LoadBalanced RestTemplate or WebClient intercepts requests, resolves the service name via DiscoveryClient (Eureka, Consul), and selects an instance using a RoundRobin strategy. It replaces the deprecated Netflix Ribbon library." },
      { q: "What is Spring Cloud Stream and how does it abstract message brokers?",
        a: "Stream defines Supplier (publisher), Function (processor), and Consumer (subscriber) beans that are automatically bound to message broker topics. A binder (Kafka or RabbitMQ) translates between the Spring abstraction and the broker's API." },
      { q: "What is the Saga pattern and how does it apply to Spring Cloud microservices?",
        a: "A Saga replaces a distributed ACID transaction with a sequence of local transactions, each publishing an event. If a step fails, compensating transactions undo prior steps. Orchestration (a central saga coordinator) or choreography (event-driven) are the two styles." },
      { q: "How do you propagate OAuth2 tokens between microservices in Spring Cloud?",
        a: "Configure each microservice as a Resource Server. Use Spring Security's token relay filter or a WebClient filter (ServerBearerExchangeFilterFunction) to forward the incoming Bearer token from upstream to downstream service calls." },
    ],
  },

  {
    name: "Hibernate",
    subtitle: "10 Q&A on ORM, caching, N+1, lazy loading, and migrations",
    accentHex: "#59666C",
    pairs: [
      { q: "What is Hibernate and how does it map Java objects to database tables?",
        a: "Hibernate is a JPA-compliant ORM. @Entity marks a class as a persistent entity. Field annotations (@Column, @Id) map attributes to columns. At startup Hibernate builds a Metamodel and can auto-generate DDL from the mappings." },
      { q: "Explain the Hibernate entity lifecycle states.",
        a: "Transient: new object, unknown to Hibernate. Persistent: associated with a Session; changes are tracked. Detached: Session closed, changes not tracked. Removed: scheduled for DELETE. Calling session.save() moves transient to persistent." },
      { q: "What is the difference between Session.get() and Session.load()?",
        a: "get() hits the database immediately and returns null if not found. load() returns a proxy (lazy placeholder) and defers the SQL hit until a field is accessed. load() throws ObjectNotFoundException at access time if the row is missing." },
      { q: "What is lazy loading and when does it cause issues?",
        a: "Lazy loading defers the SQL for associations (e.g., @OneToMany) until the collection is accessed. It causes LazyInitializationException when accessed outside an open Session. Fix with JOIN FETCH, @EntityGraph, or keeping the Session open (Open Session in View)." },
      { q: "Describe Hibernate's two levels of caching.",
        a: "L1 (Session cache) is always on; identical IDs within one Session return the same object without a second SQL query. L2 (SessionFactory cache, optional) is shared across Sessions; providers include Ehcache and Redis. @Cacheable enables it per entity." },
      { q: "How do you map a bidirectional @OneToMany relationship?",
        a: "The @OneToMany side uses mappedBy='fieldName' pointing to the owning side. The @ManyToOne side owns the foreign key column. Always update the owning side to persist changes; the inverse side is for navigation only." },
      { q: "What is the N+1 select problem and how do you fix it?",
        a: "N+1 occurs when loading N parent entities then running one query per entity to fetch a child collection. Fix with JOIN FETCH in JPQL (loads all in one query), @EntityGraph, or batch-fetching via @BatchSize on the association." },
      { q: "How does optimistic locking work in Hibernate with @Version?",
        a: "Add a @Version Long field to the entity. Hibernate includes WHERE version=? in every UPDATE. If another transaction modified the row first, the version no longer matches and Hibernate throws OptimisticLockException, preventing a lost update." },
      { q: "What is HQL and how does it differ from SQL and Criteria API?",
        a: "HQL is object-oriented: queries reference entity names and field names, not table or column names. Hibernate translates HQL to the target SQL dialect. The Criteria API constructs the same queries programmatically in a type-safe manner." },
      { q: "How do you manage schema migrations with Flyway and Hibernate?",
        a: "Set spring.jpa.hibernate.ddl-auto=validate so Hibernate only checks schema matches entities without changing it. Flyway applies versioned SQL scripts (V1__init.sql, V2__add_column.sql) in order, keeping schema changes explicit and auditable." },
    ],
  },

  {
    name: "JPA",
    subtitle: "10 Q&A on entity lifecycle, JPQL, relationships, and Spring Data",
    accentHex: "#007396",
    pairs: [
      { q: "What is JPA and how does it relate to Hibernate?",
        a: "JPA (Jakarta Persistence API) is a specification defining how Java objects map to relational data. Hibernate is the most popular JPA provider that implements the spec. Code written to the JPA API can switch providers without changes." },
      { q: "Describe the four JPA entity lifecycle states.",
        a: "New (transient): not associated with a persistence context. Managed: changes tracked, flushed to DB on commit. Detached: context closed, changes ignored. Removed: entity marked for deletion; DELETE runs at flush." },
      { q: "What does EntityManager.persist(), merge(), and remove() each do?",
        a: "persist() makes a new entity managed (INSERT on flush). merge() copies state of a detached entity onto a managed instance and returns it. remove() marks a managed entity for deletion. None issues SQL immediately — that happens at flush/commit." },
      { q: "How do you write a JPQL query and what makes it portable?",
        a: "JPQL uses entity class names and field names instead of table/column names. Example: SELECT u FROM User u WHERE u.email = :email. The JPA provider translates to the target database dialect, making queries portable across databases." },
      { q: "What is the difference between @Entity, @MappedSuperclass, and @Embeddable?",
        a: "@Entity maps to its own table. @MappedSuperclass shares mapped fields with subclass entities but has no table of its own. @Embeddable defines a value object whose columns are embedded in the owning entity's table." },
      { q: "Explain the three JPA inheritance mapping strategies.",
        a: "SINGLE_TABLE: all subclasses in one table, a discriminator column identifies the type — fast but nullable columns. JOINED: subclass-specific columns in child tables joined to the parent — normalized. TABLE_PER_CLASS: full columns in each table — no joins but no polymorphic queries." },
      { q: "How do derived query methods work in Spring Data JPA?",
        a: "Spring Data parses the method name at startup and generates JPQL. findByEmailAndActiveTrue() generates SELECT u FROM User u WHERE u.email = ?1 AND u.active = true. Supported keywords include Is, Not, Between, LessThan, OrderBy, etc." },
      { q: "What is the role of @Transactional and how does propagation work?",
        a: "REQUIRED (default) joins an existing transaction or starts one. REQUIRES_NEW always starts a new transaction, suspending the outer one. MANDATORY throws if there is no active transaction. The proxy intercepts method calls to manage commit/rollback." },
      { q: "How do you map a composite primary key in JPA?",
        a: "With @EmbeddedId: create a @Embeddable class holding the key fields and annotate the entity field with @EmbeddedId. With @IdClass: annotate the entity with @IdClass(MyKey.class) and repeat the key fields annotated @Id in the entity itself." },
      { q: "What is a JPQL DTO projection and why use it instead of returning entities?",
        a: "A constructor expression (SELECT new com.app.dto.UserDto(u.id, u.name) FROM User u) returns lightweight DTOs instead of full managed entities. This avoids loading unused columns, prevents accidental lazy-loading, and is ideal for read-only API responses." },
    ],
  },

  {
    name: "Jakarta EE",
    subtitle: "10 Q&A on CDI, JAX-RS, EJB, JMS, and enterprise security",
    accentHex: "#ED8B00",
    pairs: [
      { q: "What is Jakarta EE and how does it relate to Java EE?",
        a: "Jakarta EE is the community-governed evolution of Java EE after Oracle transferred stewardship to the Eclipse Foundation. The namespace changed from javax.* to jakarta.* in EE 9. It standardizes enterprise APIs like CDI, JAX-RS, JPA, and JMS." },
      { q: "What is CDI and what are its main bean scopes?",
        a: "CDI (Contexts and Dependency Injection) manages the lifecycle and injection of beans. @RequestScoped creates one instance per HTTP request. @SessionScoped per HTTP session. @ApplicationScoped is a singleton shared across the application." },
      { q: "How do you build a RESTful endpoint with JAX-RS?",
        a: "Annotate the class with @Path('/users') and method with @GET/@POST plus @Produces(MediaType.APPLICATION_JSON). @PathParam, @QueryParam, and @RequestBody bind URL parts and request data. Extend Application or use a Jakarta EE server's auto-scan." },
      { q: "When would you use a @Stateless EJB vs @Stateful vs @Singleton?",
        a: "@Stateless handles requests without client-specific state and is pooled — ideal for services. @Stateful maintains conversational state per client session — use for shopping carts. @Singleton has one instance per JVM and supports concurrent access control." },
      { q: "How does JMS work and what is the difference between Queue and Topic?",
        a: "A JMS Queue delivers each message to exactly one consumer (point-to-point). A Topic delivers the message to all subscribers (publish-subscribe). @MessageDriven EJBs or JMS listeners process messages asynchronously from the broker." },
      { q: "How do you configure JPA persistence in a Jakarta EE application?",
        a: "Define META-INF/persistence.xml listing the persistence unit, JTA data source, and entity classes. Inject the EntityManager with @PersistenceContext(unitName='myPU'). The container manages transactions, so use @Transactional or container-managed TX." },
      { q: "What is JNDI and how do you look up a resource by name?",
        a: "JNDI (Java Naming and Directory Interface) is a registry for server resources. @Resource(lookup='java:comp/env/jdbc/myDS') injects a DataSource directly. Programmatic lookup uses InitialContext ctx = new InitialContext(); ctx.lookup('java:...')." },
      { q: "How do Jakarta EE security mechanisms protect a JAX-RS endpoint?",
        a: "@RolesAllowed('ADMIN') on a resource method restricts access to principals with that role. The security domain, identity store (@DatabaseIdentityStoreDefinition), and authentication mechanism (@FormAuthenticationMechanismDefinition) are configured via annotations." },
      { q: "What is the difference between WAR, EAR, and executable JAR packaging?",
        a: "A WAR is a web archive deployed to a servlet container or app server. An EAR bundles multiple WARs and EJB JARs for a full app server. Modern Jakarta EE apps often use executable JARs with embedded servers (WildFly Bootable JAR, Open Liberty) for cloud deployment." },
      { q: "How do CDI interceptors differ from EJB interceptors?",
        a: "CDI interceptors are bound to beans via @InterceptorBinding, a custom annotation that marks methods to intercept. EJB interceptors use @Interceptors(MyInterceptor.class) on the bean class. CDI interceptors work on any CDI-managed bean, not just EJBs." },
    ],
  },

  {
    name: "Quarkus",
    subtitle: "10 Q&A on native image, CDI, Panache, reactive, and testing",
    accentHex: "#4695EB",
    pairs: [
      { q: "What is Quarkus and why is it designed for cloud-native environments?",
        a: "Quarkus is a Kubernetes-native Java framework that moves as much work as possible to build time. This produces extremely fast startup (< 50 ms native) and low memory footprint, which directly reduces cloud compute costs and enables efficient scale-to-zero." },
      { q: "How does Quarkus native compilation with GraalVM work?",
        a: "Quarkus performs AOT analysis at build time using GraalVM native-image. It traces reachable classes/methods, eliminates dead code, and compiles to a platform-specific binary. Reflection and dynamic proxies must be declared via metadata for native builds." },
      { q: "How does Quarkus's build-time CDI differ from standard runtime CDI?",
        a: "Quarkus resolves CDI injection points, validates beans, and generates synthetic bytecode at build time using the Jandex index. This eliminates the runtime reflection that standard CDI requires, reducing startup time and memory significantly." },
      { q: "How do you build a REST endpoint in Quarkus with RESTEasy Reactive?",
        a: "Annotate a class with @Path('/items') and methods with @GET, @POST using standard JAX-RS annotations. RESTEasy Reactive processes requests on the event loop without blocking; return Uni<T> from SmallRye Mutiny for true reactive endpoints." },
      { q: "What are Quarkus Dev Services?",
        a: "Dev Services automatically start required infrastructure (PostgreSQL, Kafka, Redis) as Testcontainers when running in dev or test mode and no explicit connection config is found. This eliminates the need for local service setup during development." },
      { q: "What are Uni and Multi types in SmallRye Mutiny?",
        a: "Uni<T> represents a single asynchronous result (0 or 1 item), similar to a promise. Multi<T> is a reactive stream of multiple items. Both are lazy — they start processing only when subscribed. Quarkus RESTEasy Reactive handles subscription automatically." },
      { q: "What is Quarkus Panache and how does it simplify JPA?",
        a: "Panache provides two patterns: Active Record (entities extend PanacheEntity and have static query methods like User.find('email', email)) and Repository (inject a PanacheRepository). Both reduce boilerplate compared to raw JPA repositories." },
      { q: "How do you consume Kafka messages in Quarkus?",
        a: "Add quarkus-smallrye-reactive-messaging-kafka. Annotate a method with @Incoming('topic-name') and define kafka channel configuration in application.properties. The framework handles consumer lifecycle, deserialization, and back-pressure automatically." },
      { q: "What happens during Quarkus's augmentation (build) phase?",
        a: "Augmentation runs annotation processors, resolves CDI, validates configuration, generates synthetic classes, and produces a pre-built bytecode store. At runtime, the app loads pre-computed data instead of re-analyzing classpath, giving near-instant startup." },
      { q: "How do you test a Quarkus application?",
        a: "@QuarkusTest starts the application on a random port for the test. Inject a REST-assured spec to call endpoints. @QuarkusIntegrationTest tests the packaged artifact (JVM JAR or native binary). Use @InjectMock to replace CDI beans with Mockito mocks." },
    ],
  },

  {
    name: "Micronaut",
    subtitle: "10 Q&A on AOT DI, data, reactive, GraalVM, and security",
    accentHex: "#1CBFCF",
    pairs: [
      { q: "What is Micronaut and why is its dependency injection compile-time?",
        a: "Micronaut uses annotation processors at build time to generate injection code. Unlike Spring's runtime reflection, there is no classpath scanning on startup. This means zero reflection overhead, instant startup, and low memory — ideal for FaaS and containers." },
      { q: "How does Micronaut's AOP work without runtime proxies?",
        a: "Micronaut generates bytecode for interceptor chains at compile time using ASM. @Around advice is woven into a generated subclass. Because no JDK dynamic proxy or CGLIB is involved, there is no class hierarchy restriction and overhead is minimal." },
      { q: "How do you define a REST controller in Micronaut?",
        a: "@Controller('/users') marks the class. @Get('/{id}'), @Post, @Put, @Delete annotate methods. @PathVariable, @QueryValue, and @Body inject URL parts and request data. Micronaut validates @Body objects with Jakarta Bean Validation automatically." },
      { q: "What is Micronaut Data and what makes it unique?",
        a: "Micronaut Data generates repository query implementations at compile time using the method name or @Query annotation, producing raw SQL/JPQL. There is no runtime proxy or reflection, making it significantly faster than Spring Data's runtime approach." },
      { q: "How does Micronaut simplify GraalVM native image creation?",
        a: "Micronaut generates GraalVM reflect-config.json, resource-config.json, and proxy-config.json metadata files automatically during compilation. This eliminates most of the manual configuration required when adding native image support to other frameworks." },
      { q: "How does Micronaut's declarative HTTP client work?",
        a: "Annotate an interface with @Client('/api') and declare methods with @Get, @Post, etc., mirroring the server-side API. Micronaut generates a compile-time implementation using Netty. Combine with @LoadBalanced for client-side load balancing." },
      { q: "How does Micronaut support reactive programming?",
        a: "Micronaut controllers can return Project Reactor Publisher<T>/Mono<T>/Flux<T> or RxJava Observable/Single. The Netty event loop handles I/O non-blocking. Annotate @ExecuteOn(TaskExecutors.IO) to shift blocking calls to a worker thread." },
      { q: "How does Micronaut's configuration system work?",
        a: "Properties from application.yml are bound at compile time to @ConfigurationProperties POJO classes. Multiple environments (test, prod) load environment-specific files. MICRONAUT_APPLICATION_NAME env var and system properties can override file values." },
      { q: "How does Micronaut Security implement JWT authentication?",
        a: "Add micronaut-security-jwt. Configure jwt.signatures.secret in application.yml. Annotate endpoints with @Secured(SecurityRule.IS_AUTHENTICATED) or @Secured('ROLE_ADMIN'). Micronaut validates Bearer tokens on every request using the configured secret or JWKS." },
      { q: "How do you write tests in Micronaut?",
        a: "@MicronautTest starts the application context for the test. Use the generated @Client interface or inject an HttpClient to call endpoints. Replace beans with @MockBean to provide mock implementations. EmbeddedServer starts a real HTTP server." },
    ],
  },

  // ── Python ──────────────────────────────────────────────────────────────
  {
    name: "Django",
    subtitle: "10 Q&A on MTV, ORM, views, authentication, and REST framework",
    accentHex: "#0C4B33",
    pairs: [
      { q: "What is Django's MTV architecture and how does it map to MVC?",
        a: "Model handles data and business logic. Template renders HTML. View acts as the controller — it processes requests, queries the Model, and returns a Template response. The URL dispatcher maps URL patterns to View functions or class-based views." },
      { q: "How does Django's ORM work and how do you create a migration?",
        a: "Define model classes with fields (CharField, IntegerField, ForeignKey). Run python manage.py makemigrations to generate migration files from model changes, then migrate to apply them. Django tracks applied migrations in the django_migrations table." },
      { q: "What is the difference between an FBV and a CBV in Django?",
        a: "A Function-Based View is a plain Python function taking a request and returning a response — simple and explicit. A Class-Based View inherits from View (or ListView, CreateView etc.) and overrides get()/post() methods, enabling reuse via mixins." },
      { q: "How does Django URL routing resolve a request to a view?",
        a: "Django iterates urlpatterns in the root urls.py, testing each path() or re_path() pattern. include() delegates to a nested urls.py. Named groups or path converters (<int:pk>) capture parts of the URL and pass them as kwargs to the view." },
      { q: "What is Django REST Framework (DRF) and what does a serializer do?",
        a: "DRF adds API tools on top of Django. A Serializer converts model instances to Python dicts (for JSON responses) and validates incoming dicts back to model instances. ModelSerializer auto-generates fields from the model definition." },
      { q: "How does Django's permission system work?",
        a: "Django auto-creates add, change, delete, view permissions for every model. @login_required or LoginRequiredMixin enforces authentication. @permission_required('app.can_publish') or PermissionRequiredMixin checks named permissions stored in the auth_permission table." },
      { q: "What is Django middleware and how does the request/response cycle work?",
        a: "Middleware classes wrap the view. process_request runs before the view (returning a response short-circuits the rest). process_response runs after. Built-in middleware handles sessions, CSRF tokens, authentication, and security headers." },
      { q: "What is template inheritance in Django and how does it work?",
        a: "A base template defines named {% block %} areas. Child templates extend it with {% extends 'base.html' %} and override blocks with their own content. The template engine replaces blocks at render time, enabling DRY layouts across pages." },
      { q: "How does Django caching work and what backends are available?",
        a: "Django's cache framework uses a unified API (cache.get/set/delete). Backends include in-memory (LocMemCache), Redis (django-redis), Memcached, and database. @cache_page(60*15) caches entire view responses; template fragment caching and low-level API give finer control." },
      { q: "How do you write tests in Django?",
        a: "Subclass django.test.TestCase (wraps each test in a transaction that rolls back). Use self.client.get('/url/') to simulate HTTP requests. APIClient (DRF) sends JSON requests. Use factories or fixtures to create test data. Run with python manage.py test." },
    ],
  },

  {
    name: "Flask",
    subtitle: "10 Q&A on WSGI, application factory, routing, and extensions",
    accentHex: "#000000",
    pairs: [
      { q: "What is Flask and how does it handle HTTP requests as a WSGI app?",
        a: "Flask is a micro-framework built on Werkzeug (WSGI toolkit) and Jinja2. When a WSGI server calls the Flask app with an environ dict, Flask parses it into a Request, routes to the matching view function, and returns a WSGI response tuple." },
      { q: "What is the application factory pattern and why is it recommended?",
        a: "Instead of creating app = Flask(__name__) at module level, a create_app() function instantiates Flask, registers blueprints, and initializes extensions. This enables multiple app instances with different configs (e.g., for testing) and avoids circular imports." },
      { q: "How does Flask routing work with Blueprints?",
        a: "@app.route('/path') registers a URL rule and view function on the Flask app. Blueprint groups related routes under a prefix: bp = Blueprint('auth', __name__, url_prefix='/auth') and is registered via app.register_blueprint(bp) in create_app." },
      { q: "How does Flask-SQLAlchemy manage the database session?",
        a: "Flask-SQLAlchemy binds a SQLAlchemy Session to the request context. db.session is available within a request and is automatically committed or rolled back at the end. Use db.session.add(obj) and db.session.commit() to persist changes." },
      { q: "What is Flask's request context and what does it push?",
        a: "Pushing a request context makes request (current HTTP request), g (per-request scratch namespace), and current_app (the Flask app) available as proxies. Contexts are pushed automatically for HTTP requests; push manually in tests or CLI commands." },
      { q: "How does Flask-Login manage user sessions?",
        a: "Flask-Login stores the user's ID in a signed cookie session. @login_required checks if current_user.is_authenticated before each request. The user_loader callback receives the ID from the session and returns the User object from your database." },
      { q: "How do you build a JSON API in Flask?",
        a: "Return jsonify(data) from a view function for a JSON response. For structured APIs, Flask-RESTful or Flask-RESTX provides Resource classes with get/post methods and automatic input parsing with reqparse or marshmallow schemas." },
      { q: "How do you handle file uploads securely in Flask?",
        a: "Access the file via request.files['field']. Always call secure_filename() on the filename to prevent directory traversal. Validate MIME type and extension. Store outside the web root or in object storage. Limit MAX_CONTENT_LENGTH in the config." },
      { q: "How does Flask integrate with Celery for background tasks?",
        a: "Create a Celery instance configured with Flask's config (CELERY_BROKER_URL). Define tasks with @celery.task. In the task function, push an app context (with app.app_context()) to access Flask extensions like db. Call task.delay() to enqueue." },
      { q: "How do you test a Flask view function?",
        a: "Use app.test_client() to send requests without a server. client.get('/path') returns a response with status_code and data. Use app.test_request_context() to test functions that use request context. pytest-flask provides fixtures for test_client and app." },
    ],
  },

  {
    name: "FastAPI",
    subtitle: "10 Q&A on type hints, Pydantic, async, DI, and OpenAPI",
    accentHex: "#009688",
    pairs: [
      { q: "How do Python type hints drive FastAPI's validation and docs?",
        a: "FastAPI reads function parameter annotations at import time and builds JSON Schema from them using Pydantic. It validates incoming requests against the schema, coerces types, and populates the OpenAPI spec — all from the same source of truth." },
      { q: "What is a Pydantic model and how does it validate data?",
        a: "A Pydantic BaseModel subclass defines fields with Python type annotations. On instantiation Pydantic validates each field, coerces compatible types (e.g., '3' to int), and raises ValidationError with detailed messages if validation fails." },
      { q: "How does FastAPI's dependency injection work with Depends()?",
        a: "A dependency is any callable annotated in a parameter as Depends(get_db). FastAPI calls it before the route handler, resolves nested Depends(), and injects the result. yield-based dependencies act as context managers for setup/teardown (e.g., DB sessions)." },
      { q: "How does FastAPI handle path operation ordering to avoid shadowing?",
        a: "FastAPI registers routes in declaration order. A fixed path /users/me must be declared before the parameterized /users/{user_id}; otherwise the router matches 'me' as a user_id value. Declare more specific routes first." },
      { q: "What is the difference between async def and def in a FastAPI route?",
        a: "async def routes run on the event loop — never block them with synchronous I/O. def routes are automatically run in a threadpool by FastAPI so they don't block the event loop. Use async def with await for async libraries; def for synchronous blocking code." },
      { q: "How do you implement OAuth2 password flow with JWT in FastAPI?",
        a: "Use OAuth2PasswordBearer to extract tokens from Authorization headers. On login, verify credentials, create a JWT with python-jose encoding claims and expiry, and return it. Protect routes with Depends(get_current_user) which decodes and validates the token." },
      { q: "How does FastAPI auto-generate OpenAPI documentation?",
        a: "FastAPI builds an OpenAPI 3 schema from route decorators, type annotations, and Pydantic models at startup. It serves interactive Swagger UI at /docs and ReDoc at /redoc. Use response_model, tags, and summary arguments to customize the spec." },
      { q: "How do you add CORS support to a FastAPI application?",
        a: "Add CORSMiddleware: app.add_middleware(CORSMiddleware, allow_origins=['https://myfrontend.com'], allow_methods=['*'], allow_headers=['*']). FastAPI handles preflight OPTIONS requests automatically. Use allow_origins=['*'] only in development." },
      { q: "How do you use SQLAlchemy async sessions with FastAPI?",
        a: "Use create_async_engine with an async driver (asyncpg for PostgreSQL). Define a get_db dependency that yields an AsyncSession. Annotate all repository methods with async and use await session.execute(stmt). Return results with scalars().all()." },
      { q: "How do you test FastAPI endpoints with TestClient?",
        a: "Instantiate TestClient(app). Call client.get('/path') or client.post('/path', json={...}) synchronously. Override dependencies for tests using app.dependency_overrides[get_db] = override_get_db. Use pytest fixtures to manage setup and teardown." },
    ],
  },

  {
    name: "Celery",
    subtitle: "10 Q&A on brokers, task routing, retries, Beat, and testing",
    accentHex: "#A9CC51",
    pairs: [
      { q: "What is Celery and what role does a message broker play?",
        a: "Celery is a distributed task queue. When you call task.delay(), Celery serializes the call and sends it to a broker (Redis or RabbitMQ). Worker processes consume messages from the broker, execute tasks, and optionally store results in a backend." },
      { q: "What is the difference between delay() and apply_async()?",
        a: "task.delay(arg1, arg2) is shorthand for apply_async((arg1, arg2)). apply_async gives you full control: countdown (delay in seconds), eta (absolute time), queue name, priority, retry policy, and task id override." },
      { q: "How does Celery handle task retries and what is autoretry_for?",
        a: "Call self.retry(exc=e, countdown=5, max_retries=3) inside a task on failure. @app.task(autoretry_for=(RequestException,), retry_backoff=True, max_retries=5) automates this: Celery retries on the listed exceptions with exponential backoff." },
      { q: "What is Celery Beat and how do you schedule a periodic task?",
        a: "Celery Beat is a scheduler that enqueues tasks on a schedule. Define entries in beat_schedule: {'run-daily': {'task': 'app.tasks.cleanup', 'schedule': crontab(hour=0, minute=0)}}. Beat requires a persistent store (db or Redis) to track last-run times." },
      { q: "What are Celery chains, groups, and chords?",
        a: "A chain(t1.s(), t2.s()) pipes output of t1 as input to t2. A group([t1.s(), t2.s()]) runs tasks in parallel. A chord(group, callback) runs the group in parallel then passes all results to callback — useful for map-reduce-style workflows." },
      { q: "How do you route tasks to specific queues in Celery?",
        a: "Define queues in the task_queues config. Set task_routes = {'myapp.tasks.heavy': {'queue': 'heavy_queue'}}. Start workers with --queues heavy_queue to consume from that queue. This segregates slow tasks from fast ones for independent scaling." },
      { q: "How does Celery handle task result storage?",
        a: "Set result_backend = 'redis://localhost' (or 'db+postgresql://...'). After completion, AsyncResult(task_id).get() blocks until the result is available. Results are stored for result_expires seconds (default 24 h) then purged." },
      { q: "What is the Celery Flower monitoring dashboard?",
        a: "Flower is a real-time web UI for Celery. Run celery -A app flower. It displays active workers, task rates, queue lengths, failed task details, and lets you revoke pending tasks or rate-limit workers from the browser." },
      { q: "How do you test Celery tasks without a running broker?",
        a: "Set task_always_eager = True in the test config. This causes .delay() and .apply_async() to execute the task synchronously in the calling process without a broker. For unit tests of task logic only, call the function directly: mytask(args) skipping Celery entirely." },
      { q: "What are Celery soft and hard time limits?",
        a: "soft_time_limit raises SoftTimeLimitExceeded inside the task, giving it a chance to clean up. hard time_limit (task_time_limit) sends SIGKILL to the worker process if the task exceeds the limit, with no cleanup possible. Prefer soft limits." },
    ],
  },

  {
    name: "Tornado",
    subtitle: "10 Q&A on IOLoop, async handlers, WebSockets, and testing",
    accentHex: "#EE3244",
    pairs: [
      { q: "What is Tornado and what makes it suited for high-concurrency use cases?",
        a: "Tornado is a Python web framework and async networking library with a non-blocking I/O model. A single-threaded IOLoop handles thousands of concurrent connections efficiently, avoiding the thread-per-request overhead of traditional WSGI servers." },
      { q: "How does the Tornado IOLoop work with modern Python asyncio?",
        a: "Since Tornado 5, the IOLoop is built on top of asyncio's event loop. tornado.ioloop.IOLoop.current() returns the current loop. You can mix Tornado coroutines with native asyncio code. asyncio.run() starts the loop for both frameworks simultaneously." },
      { q: "How do you define a request handler in Tornado?",
        a: "Subclass tornado.web.RequestHandler and override HTTP method coroutines: async def get(self), async def post(self). Use self.get_argument(), self.request.body, and self.write() to read input and write output. Map it to a URL in the Application URL spec." },
      { q: "What is the difference between @tornado.gen.coroutine and async/await?",
        a: "@gen.coroutine with yield was Tornado's pre-3.5 async style and still works. Native async def / await is preferred in Python 3.5+ and is more readable. Under the hood both produce the same IOLoop-driven coroutine, so they interoperate freely." },
      { q: "How does Tornado's WebSocket handler work?",
        a: "Subclass WebSocketHandler. Override open() (connection established), on_message(message) (data received), and on_close() (connection closed). Call self.write_message(data) to send to the client. Map the handler to a URL like any RequestHandler." },
      { q: "How does Tornado's template engine handle auto-escaping?",
        a: "Tornado templates auto-escape HTML output by default, preventing XSS. Use {% raw expr %} to render unescaped content. Unlike Jinja2, Tornado templates are compiled to Python code, making them fast but with a smaller feature set." },
      { q: "How do you implement user authentication in Tornado?",
        a: "Override get_current_user() to return the authenticated user (e.g., decoded from a signed cookie with self.get_secure_cookie). Decorate handlers with @tornado.web.authenticated, which redirects unauthenticated users to login_url." },
      { q: "How do you enable SSL/TLS in Tornado's HTTPServer?",
        a: "Pass ssl_options={'certfile': '/path/cert.pem', 'keyfile': '/path/key.pem'} to HTTPServer(app, ssl_options=...). HTTPServer can also accept an ssl.SSLContext object for advanced configuration like client certificate verification." },
      { q: "How do you run Tornado across multiple CPU cores?",
        a: "Call tornado.process.fork_processes(0) to fork one worker per CPU core. Each process runs its own IOLoop. Alternatively, start multiple Tornado processes behind a load balancer. Tornado's non-blocking model means fewer processes are needed than with WSGI." },
      { q: "How do you write async tests in Tornado?",
        a: "Subclass AsyncHTTPTestCase and override get_app() to return your Application. Use self.fetch('/path') which blocks until the response arrives. @gen_test(timeout=5) decorates test methods that are coroutines, running them on the IOLoop." },
    ],
  },

  // ── Node.js ──────────────────────────────────────────────────────────────
  {
    name: "Node.js",
    subtitle: "10 Q&A on event loop, streams, clustering, and async patterns",
    accentHex: "#339933",
    pairs: [
      { q: "What are the phases of the Node.js event loop?",
        a: "Timers (setTimeout/setInterval callbacks), Pending callbacks (I/O errors), Idle/Prepare (internal), Poll (I/O events, blocking if nothing else pending), Check (setImmediate callbacks), Close callbacks. Each phase has a queue that drains before moving to the next." },
      { q: "What is the difference between process.nextTick(), setImmediate(), and setTimeout(fn,0)?",
        a: "process.nextTick() runs before any I/O callbacks, at the end of the current operation — highest priority. setImmediate() runs in the Check phase after I/O callbacks. setTimeout(fn,0) runs in the Timers phase, which may be after I/O depending on timing." },
      { q: "How does Node.js clustering work?",
        a: "cluster.fork() creates child worker processes that all share one TCP server port. The master distributes incoming connections (round-robin on most OS). Each worker has its own V8 instance and event loop. cluster.isMaster / cluster.isWorker distinguish roles." },
      { q: "What is backpressure in Node.js streams and why does it matter?",
        a: "Backpressure occurs when a Writable stream cannot consume data as fast as a Readable produces it. If ignored, memory grows unboundedly. pipe() handles backpressure automatically by pausing the Readable when the Writable's internal buffer is full." },
      { q: "What is the difference between CommonJS require() and ES module import?",
        a: "CommonJS uses require() (synchronous, dynamic, returns a copy). ES modules use import/export (static, asynchronous, live bindings). Node.js supports both: .cjs for CommonJS, .mjs or 'type':'module' in package.json for ESM. They cannot be mixed without interop care." },
      { q: "How do you handle errors in async Node.js code?",
        a: "Callback style: check the first err argument. Promises: .catch() or try/catch in async functions. Unhandled rejections can crash the process in newer Node versions. Use process.on('uncaughtException') and 'unhandledRejection' as last-resort safety nets." },
      { q: "What is the Node.js Buffer class and when is it needed?",
        a: "Buffer represents fixed-size binary data outside the V8 heap (no GC pressure). It is used when working with raw TCP streams, file system data, or binary protocols where encoding is not UTF-8. Buffer.from(data, 'base64') and buf.toString('hex') convert formats." },
      { q: "When should you use Node.js worker_threads vs cluster?",
        a: "cluster forks separate processes sharing a port — ideal for scaling I/O-bound HTTP servers across CPUs. worker_threads run in the same process with shared ArrayBuffer memory — ideal for CPU-intensive tasks (image processing, cryptography) without IPC serialization overhead." },
      { q: "What operations use the libuv threadpool in Node.js?",
        a: "File system operations (fs.*), DNS lookups (dns.lookup), and crypto operations (bcrypt, randomBytes in older versions) use the 4-thread libuv pool by default. Increase with UV_THREADPOOL_SIZE=8 if many blocking ops run concurrently." },
      { q: "How do you profile a Node.js application for performance?",
        a: "Use node --prof app.js to generate a V8 isolate log, then node --prof-process to summarize hot functions. Chrome DevTools (node --inspect) provides a CPU flame graph. clinic.js doctor and flame give guided profiling for production diagnostics." },
    ],
  },

  {
    name: "Express.js",
    subtitle: "10 Q&A on middleware pipeline, routing, security, and testing",
    accentHex: "#000000",
    pairs: [
      { q: "How does the Express middleware pipeline work?",
        a: "Middleware functions (req, res, next) are executed in registration order. Calling next() passes control to the next function. Calling res.send() or res.json() ends the response. If next(err) is called, Express jumps to the nearest 4-parameter error handler." },
      { q: "What is the difference between app.use(), app.get(), and app.all()?",
        a: "app.use(path?, fn) runs for every HTTP method matching the path prefix (including sub-paths). app.get(path, fn) matches only GET on the exact path. app.all(path, fn) matches all methods on the exact path. app.use() is the foundation for middleware." },
      { q: "How do you implement error-handling middleware in Express?",
        a: "Define a function with exactly four parameters (err, req, res, next). Register it last, after all routes. Express detects the 4-param signature and skips it for normal requests. Call next(err) from a route or middleware to activate it." },
      { q: "How does Express Router modularize routes?",
        a: "const router = express.Router() creates a mini-app. Define routes on router (router.get('/users', handler)). Mount it on the main app with app.use('/api', router). This keeps route logic separated by domain and avoids a monolithic routes file." },
      { q: "How do you parse different request body types in Express?",
        a: "Use express.json() to parse JSON bodies and express.urlencoded({extended:true}) for HTML form data. Both are built into Express 4.16+. For multipart/form-data (file uploads) use multer middleware, which processes streams and populates req.file / req.files." },
      { q: "How does session management work in Express?",
        a: "express-session sets a signed session cookie and stores session data in a backend store (MemoryStore for dev, connect-redis or connect-pg-simple for production). req.session.user = user persists data across requests for the session lifetime." },
      { q: "What does the Helmet middleware do for security?",
        a: "Helmet sets security-relevant HTTP response headers: Content-Security-Policy, X-Frame-Options (clickjacking), X-XSS-Protection, Strict-Transport-Security (HSTS), and others. Call app.use(helmet()) early in the middleware stack before routes." },
      { q: "How do you connect Express to MongoDB using Mongoose?",
        a: "Call mongoose.connect(uri) once at app startup. Define a Schema and Model (mongoose.model('User', userSchema)). Use User.find({active: true}), User.findById(id), and user.save() to query and persist. Mongoose validates data against the schema before writing." },
      { q: "How do you implement rate limiting in Express?",
        a: "Use express-rate-limit: const limiter = rateLimit({windowMs: 15*60*1000, max: 100}). Apply with app.use('/api/', limiter). By default it uses in-memory storage; for distributed deployments use rate-limit-redis to share counts across server instances." },
      { q: "How do you write integration tests for Express routes using Supertest?",
        a: "import request from 'supertest'; const res = await request(app).post('/users').send({name:'Alice'}). Supertest starts the Express app on an ephemeral port, sends the request, and resolves with the response. No need to listen on a real port." },
    ],
  },

  {
    name: "NestJS",
    subtitle: "10 Q&A on modules, DI, guards, pipes, interceptors, and testing",
    accentHex: "#E0234E",
    pairs: [
      { q: "How does NestJS's modular architecture work?",
        a: "A Module groups related controllers and providers. @Module({imports, controllers, providers, exports}) defines its boundaries. The AppModule is the root. Feature modules import other modules and export providers they want to share with importers." },
      { q: "How does NestJS's dependency injection container work?",
        a: "Providers declared in a module's providers array are added to the DI container. NestJS resolves constructor injection by type token. Provider scope can be DEFAULT (singleton per module), REQUEST (new instance per request), or TRANSIENT (new per injection)." },
      { q: "What are NestJS decorators and how do they work?",
        a: "Decorators are TypeScript metadata annotations. @Get('/path') stores route metadata on the method. @Body(), @Param(), @Query() bind request data. NestJS's RouterExplorer reads this metadata at startup to register Express/Fastify routes automatically." },
      { q: "How do NestJS Pipes work and how do you validate request bodies?",
        a: "Pipes transform or validate data before it reaches the handler. Global ValidationPipe with whitelist:true strips undeclared properties and transforms payloads. class-validator decorators (@IsEmail(), @MinLength(8)) on DTO classes define validation rules." },
      { q: "What are NestJS Guards and how do you implement RBAC?",
        a: "A Guard implements CanActivate. Return true to allow or false to deny. @UseGuards(RolesGuard) applies it to a controller or method. The guard reads @Roles('admin') metadata via Reflector and checks the authenticated user's roles against the required roles." },
      { q: "What is a NestJS exception filter and how does it standardize error responses?",
        a: "Implement ExceptionFilter, annotate with @Catch(MyException), and define catch(exception, host). It intercepts thrown exceptions and returns a structured JSON error response. Register globally with app.useGlobalFilters(new MyFilter()) for consistent API error shapes." },
      { q: "What are NestJS interceptors used for?",
        a: "Interceptors wrap request handling before and after. The intercept(ctx, next) method calls next.handle() which returns an Observable. Use RxJS operators to transform the response (map), measure execution time, add caching, or log requests and responses." },
      { q: "How do you integrate TypeORM with NestJS?",
        a: "Import TypeOrmModule.forRoot(options) in AppModule. Import TypeOrmModule.forFeature([UserEntity]) in the feature module to get a repository. Inject InjectRepository(UserEntity) private userRepo: Repository<UserEntity> into services." },
      { q: "How does NestJS support microservice communication?",
        a: "NestJS microservices use ClientProxy to send messages and emit events. Transport strategies include TCP, Redis, NATS, Kafka, and RabbitMQ. @MessagePattern('pattern') on a handler receives messages. Both request-response and event-driven patterns are supported." },
      { q: "How do you write unit tests in NestJS?",
        a: "Use Test.createTestingModule({providers: [MyService, {provide: Repo, useValue: mockRepo}]}).compile(). This creates a mini DI container. Call module.get(MyService) to retrieve the instance. Jest spies and mocks replace real dependencies." },
    ],
  },

  {
    name: "Fastify",
    subtitle: "10 Q&A on performance, plugin system, schema validation, and hooks",
    accentHex: "#000000",
    pairs: [
      { q: "What makes Fastify faster than Express.js?",
        a: "Fastify uses a radix-tree (find-my-way) router that is O(1) per segment vs Express's regex iteration. JSON serialization uses fast-json-stringify which generates compiled serializers from JSON Schema, skipping generic JSON.stringify entirely." },
      { q: "How does Fastify's plugin system and encapsulation work?",
        a: "fastify.register(plugin, options) adds a plugin in an encapsulated scope. Decorators and hooks added inside a plugin are scoped to it and its children. fastify-plugin() opts out of encapsulation, making decorators visible to the parent scope." },
      { q: "How does Fastify's JSON Schema validation protect routes?",
        a: "Attach a schema object to route options: {schema:{body:{type:'object',required:['name'],properties:{name:{type:'string'}}}}}. Fastify uses ajv to validate incoming requests before the handler runs and returns 400 with a structured error if validation fails." },
      { q: "What are Fastify lifecycle hooks and when do they run?",
        a: "Hooks run at named stages: onRequest (first, before parsing), preParsing, preValidation, preHandler (after validation), preSerialization, onSend (before response), onResponse (after). Use fastify.addHook('preHandler', fn) or route-level hooks." },
      { q: "How do you implement JWT authentication in Fastify?",
        a: "@fastify/jwt decorates the instance with fastify.jwt.sign(payload) and fastify.jwt.verify(token). Register fastify.authenticate as a preHandler hook on protected routes. The plugin reads the Bearer token from the Authorization header automatically." },
      { q: "How does Fastify's response serialization work?",
        a: "Define a response JSON Schema in the route schema ({schema:{response:{200:{...}}}}}). Fastify generates a compiled serializer with fast-json-stringify at startup. This is ~3x faster than JSON.stringify and strips fields not in the schema, preventing data leakage." },
      { q: "How do you add security headers and rate limiting in Fastify?",
        a: "@fastify/helmet sets security headers (CSP, X-Frame-Options). @fastify/rate-limit tracks requests per IP using in-memory or Redis counters and returns 429 when limits are exceeded. Both are registered as plugins with fastify.register()." },
      { q: "What are Fastify decorators and how do you use them?",
        a: "fastify.decorate('redis', redisClient) adds a property to the Fastify instance. fastify.decorateRequest('user', null) adds a property to every Request object. This allows plugins to attach shared state (DB connections, auth context) accessible in handlers." },
      { q: "How do you integrate PostgreSQL in a Fastify application?",
        a: "@fastify/postgres wraps pg pool and adds fastify.pg to the instance. For an ORM, use Prisma (prismaClient as a decorator) or Drizzle. Always close connections on shutdown using fastify.addHook('onClose', () => pool.end())." },
      { q: "How do you write tests for Fastify routes?",
        a: "fastify.inject({method:'POST', url:'/users', payload:{name:'Alice'}}) sends a request in-process without a TCP connection. It resolves with a LightMyRequest response. Use Jest or tap assertions on response.statusCode and JSON.parse(response.body)." },
    ],
  },

  {
    name: "Koa",
    subtitle: "10 Q&A on async middleware, ctx object, routing, and error handling",
    accentHex: "#33333D",
    pairs: [
      { q: "What is Koa and how does it improve on Express's middleware model?",
        a: "Koa is built by the Express team using async/await instead of callbacks. Its cascade middleware uses async functions and await next(), giving you clean before/after control without nesting. Koa has no bundled routing or body parsing — use separate packages." },
      { q: "How does Koa's cascading middleware work in practice?",
        a: "Middleware runs sequentially until next() is awaited, then in reverse after. Code before await next() runs on the way in; code after runs on the way out. This makes it trivial to measure duration: record time before next(), log delta after." },
      { q: "What is the Koa ctx (context) object?",
        a: "ctx bundles the Node.js request and response: ctx.request (Koa's enhanced request), ctx.response, ctx.params (router), ctx.state (app-level data store), and shortcuts like ctx.body, ctx.status, ctx.get(), ctx.set(). Middleware reads and writes via ctx." },
      { q: "What is the difference between ctx.body and ctx.response.body?",
        a: "They are the same underlying property — ctx.body is an alias for ctx.response.body. Koa delegates many properties from ctx directly to ctx.request and ctx.response as shortcuts. Setting ctx.body to an object automatically sends JSON with the correct Content-Type." },
      { q: "How do you handle routing in Koa with koa-router?",
        a: "const router = new Router(); router.get('/users/:id', async ctx => {ctx.body = await getUser(ctx.params.id)}). Use router.routes() and router.allowedMethods() as middleware on the Koa app. Groups can share prefixes with router.prefix('/api')." },
      { q: "How does centralized error handling work in Koa?",
        a: "Add an error-catching middleware as the first in the stack: try { await next() } catch (err) { ctx.status = err.status || 500; ctx.body = {error: err.message}; ctx.app.emit('error', err, ctx) }. It catches errors from all downstream middleware." },
      { q: "How do you parse request bodies in Koa?",
        a: "koa-bodyparser adds ctx.request.body for JSON and URL-encoded bodies. Register it before routes: app.use(bodyParser()). For multipart file uploads use @koa/multer or busboy-based middleware. koa-body combines both." },
      { q: "How do you manage sessions in Koa?",
        a: "koa-session adds ctx.session to store per-user data in a signed cookie. For large payloads configure an external store (Redis). app.keys = ['secret'] must be set for cookie signing. Session data is loaded on request and saved on response." },
      { q: "How do you serve static files in Koa?",
        a: "koa-static(root, options) creates middleware that checks if the URL maps to a file in root, reads it, and sets the correct Content-Type. Register it early so static file requests bypass the router entirely. Set maxage for browser caching." },
      { q: "How do you test a Koa application?",
        a: "Use supertest: const server = app.callback(); await request(server).get('/users').expect(200). app.callback() returns a plain http.RequestListener without starting a server, allowing tests to use ephemeral ports managed by supertest." },
    ],
  },

  // ── .NET / Go / PHP / Ruby ───────────────────────────────────────────────
  {
    name: "ASP.NET Core",
    subtitle: "10 Q&A on pipeline, DI lifetimes, Minimal API, auth, and testing",
    accentHex: "#512BD4",
    pairs: [
      { q: "What is the ASP.NET Core request pipeline and how do you add middleware?",
        a: "The pipeline is a chain of middleware delegates in Program.cs. app.UseRouting(), app.UseAuthentication(), and app.UseAuthorization() must be in that order. app.Use(async (ctx, next) => {...; await next(ctx)}) inserts custom middleware." },
      { q: "What are the three DI service lifetimes and what are captive dependency risks?",
        a: "Singleton lives for the app lifetime. Scoped lives per HTTP request. Transient is created fresh each injection. A captive dependency occurs when a Singleton injects a Scoped service — the Scoped service is never disposed correctly. ASP.NET Core detects this at startup." },
      { q: "How does ASP.NET Core attribute routing work?",
        a: "[Route('api/users')] on a controller and [HttpGet('{id:int}')] on an action define the full route template. Route constraints like :int, :guid, and :minlength(3) validate segments. [ApiController] enforces model binding and returns 400 on validation failure." },
      { q: "What is the difference between [FromBody], [FromQuery], and [FromRoute]?",
        a: "[FromBody] reads from the JSON request body (only one per action). [FromQuery] reads query string parameters. [FromRoute] reads named route template segments. Without explicit attributes, [ApiController] infers: complex types from body, simple types from query/route." },
      { q: "What is Minimal API in ASP.NET Core 6+ and when should you use it?",
        a: "Minimal API uses lambda-based route handlers (app.MapGet('/users/{id}', (int id) => ...)) instead of controllers. It is less structured but has lower overhead and works well for microservices or simple CRUD APIs where the MVC controller ceremony is unnecessary." },
      { q: "How do you add JWT Bearer authentication to ASP.NET Core?",
        a: "Call builder.Services.AddAuthentication().AddJwtBearer(opts => {opts.TokenValidationParameters = new() {ValidIssuer=..., IssuerSigningKey=...}}). Add app.UseAuthentication() and app.UseAuthorization() in the pipeline. Protect endpoints with [Authorize]." },
      { q: "What is the Options pattern in ASP.NET Core configuration?",
        a: "Define a class MySettings with properties. Call builder.Services.Configure<MySettings>(builder.Configuration.GetSection('MySettings')). Inject IOptions<MySettings> (singleton snapshot), IOptionsSnapshot<MySettings> (scoped, reloads per request), or IOptionsMonitor (live)." },
      { q: "What is Entity Framework Core and how does Add-Migration work?",
        a: "EF Core is Microsoft's ORM. DbContext represents a session with the database. Define DbSet<T> properties for each entity. dotnet ef migrations add AddUserTable generates a migration C# file based on the difference between the model and the last migration." },
      { q: "What are action filters in ASP.NET Core and what can they do?",
        a: "IActionFilter runs OnActionExecuting (before the action) and OnActionExecuted (after). Use them for logging, validation, or caching. [ResponseCache], [ValidateAntiForgeryToken] are built-in filters. Register globally in AddControllers(opts => opts.Filters.Add(...))." },
      { q: "How do you write integration tests for ASP.NET Core?",
        a: "Use WebApplicationFactory<Program> to spin up the app in-process. Call factory.CreateClient() to get an HttpClient that sends requests to the test server. Override services with factory.WithWebHostBuilder(b => b.ConfigureTestServices(...)). No real port is needed." },
    ],
  },

  {
    name: ".NET",
    subtitle: "10 Q&A on CLR, GC, async/await, LINQ, generics, and testing",
    accentHex: "#512BD4",
    pairs: [
      { q: "What is the CLR and how does JIT compilation work in .NET?",
        a: "The Common Language Runtime is the virtual machine that executes .NET code. Source is compiled to CIL (Common Intermediate Language). At runtime the JIT compiler translates CIL to native machine code on first execution. The result is cached for subsequent calls." },
      { q: "What is the difference between .NET Framework, .NET Core, and .NET 5+?",
        a: ".NET Framework (Windows only, ships with Windows). .NET Core (cross-platform, performant, open-source, side-by-side). .NET 5+ unified both into a single cross-platform platform with yearly releases. .NET Framework still exists for legacy Windows apps but receives only security patches." },
      { q: "How does the .NET Garbage Collector manage object generations?",
        a: "Objects start in Gen0 (collected frequently, cheaply). Objects that survive Gen0 are promoted to Gen1, then Gen2. Large objects (>85 KB) go directly to the Large Object Heap and are collected with Gen2. Shorter-lived Gen0 collection pauses are the fastest." },
      { q: "How does async/await work in .NET and what does ConfigureAwait(false) do?",
        a: "await suspends the method and returns a Task. When the awaited operation completes, execution resumes — on the original SynchronizationContext by default (e.g., UI thread). ConfigureAwait(false) skips context capture, avoiding deadlocks in library code." },
      { q: "What is LINQ and what is deferred execution?",
        a: "LINQ provides a query syntax over any IEnumerable<T>. Deferred execution means the query is not evaluated until you enumerate it (foreach, .ToList(), .Count()). This enables composing queries without materializing intermediate results." },
      { q: "What is the difference between a value type and a reference type in .NET?",
        a: "Value types (struct, int, bool, DateTime) are stored inline in the stack or in the object containing them. Reference types (class) store a pointer on the stack; the object lives on the heap. Assigning a value type copies data; assigning a reference type copies the pointer." },
      { q: "Why are generics preferred over object-based collections in .NET?",
        a: "List<int> stores ints directly without boxing. List<object> boxes each int into a heap-allocated object, adding GC pressure and casting overhead. Generics are also type-safe: the compiler rejects List<string>.Add(42) at compile time without a runtime cast." },
      { q: "What is Span<T> and when should you use it?",
        a: "Span<T> is a stack-allocated view of a contiguous memory region (array, string, native memory). It enables sub-slice operations without allocation: span.Slice(5, 10) does not copy. Use it in hot parsing code (HTTP headers, CSV rows) to eliminate allocations." },
      { q: "How does the .NET dependency injection container work?",
        a: "Register services with builder.Services.AddSingleton<IFoo, Foo>(). The container builds a ServiceProvider that resolves constructor parameters recursively. Call provider.GetRequiredService<IFoo>() or let the framework inject via constructor injection in controllers and services." },
      { q: "How do you write unit tests in .NET with xUnit and Moq?",
        a: "Annotate test methods with [Fact] or [Theory] with [InlineData]. Use Mock<IService>() to create a mock. Setup behavior with mock.Setup(s => s.GetData()).Returns(fakeData). Pass mock.Object to the class under test. Assert with Assert.Equal, Assert.Throws." },
    ],
  },

  {
    name: "Entity Framework Core",
    subtitle: "10 Q&A on migrations, relationships, loading, concurrency, and testing",
    accentHex: "#512BD4",
    pairs: [
      { q: "What is the Code-First approach in Entity Framework Core?",
        a: "You define C# entity classes and a DbContext with DbSet<T> properties. EF Core derives the database schema from the model. dotnet ef migrations add creates migration files, and dotnet ef database update applies them — no hand-written SQL needed." },
      { q: "How do you configure DbContext for dependency injection in ASP.NET Core?",
        a: "Call builder.Services.AddDbContext<AppDbContext>(opts => opts.UseSqlServer(connectionString)) in Program.cs. ASP.NET Core injects a Scoped DbContext per request. Avoid Singleton DbContext because it's not thread-safe." },
      { q: "How do you define a one-to-many relationship using Fluent API?",
        a: "In OnModelCreating: modelBuilder.Entity<Order>().HasMany(o => o.Items).WithOne(i => i.Order).HasForeignKey(i => i.OrderId).OnDelete(DeleteBehavior.Cascade). Fluent API takes precedence over Data Annotations and keeps entity classes clean." },
      { q: "What is the difference between eager, lazy, and explicit loading?",
        a: "Eager: Include(o => o.Items) adds a JOIN in the query. Lazy: accessing a navigation property on a proxy triggers an extra SQL query automatically (requires UseLazyLoadingProxies). Explicit: context.Entry(order).Collection(o => o.Items).Load() fires one SQL on demand." },
      { q: "How do EF Core migrations work and how do you roll one back?",
        a: "dotnet ef migrations add Name generates a C# migration class with Up() and Down() methods. dotnet ef database update applies pending migrations in order. To roll back: dotnet ef database update PreviousMigrationName, which runs Down() of the removed migration." },
      { q: "How do you execute raw SQL safely in EF Core?",
        a: "context.Database.ExecuteSqlRaw('UPDATE Orders SET Status=0 WHERE Id={0}', id) uses parameterized binding. Use FromSqlRaw to return entities: context.Orders.FromSqlRaw('SELECT * FROM Orders WHERE Status={0}', status). Never concatenate untrusted strings." },
      { q: "What is optimistic concurrency and how do you implement it in EF Core?",
        a: "Add a [ConcurrencyCheck] property or a [Timestamp] byte[] property to the entity. EF Core includes the original value in WHERE status=@original in UPDATE statements. If another transaction modified the row, the WHERE clause matches 0 rows and EF Core throws DbUpdateConcurrencyException." },
      { q: "What is the Repository pattern over EF Core and when is it worth it?",
        a: "A Repository wraps DbContext with a domain-specific interface (IUserRepository). Benefits: testability via in-memory implementations and hiding EF details. Drawbacks: duplicates IQueryable composition that DbContext already provides. Use it for complex domain models; skip it for CRUD-heavy apps." },
      { q: "What are compiled queries in EF Core and when do they help?",
        a: "EF Core translates LINQ to SQL on every query call, including query plan compilation. EF.CompileQuery((AppDb db, int id) => db.Users.Where(u => u.Id == id)) precompiles the plan. Useful in hot paths where the same parameterized query runs thousands of times per second." },
      { q: "How do you test EF Core with an in-memory provider vs SQLite in-memory?",
        a: "UseInMemoryDatabase is not a relational DB — it ignores SQL constraints and cascades. SQLite in-memory (UseSqlite('Data Source=:memory:')) is a real SQL engine supporting constraints, migrations, and relational behavior, making it more reliable for integration tests." },
    ],
  },

  {
    name: "Blazor",
    subtitle: "10 Q&A on component lifecycle, data binding, JS interop, and state",
    accentHex: "#512BD4",
    pairs: [
      { q: "What is Blazor and what are the differences between its hosting models?",
        a: "Blazor Server runs C# on the server and uses a SignalR WebSocket to push UI diffs to the browser — low initial load, requires persistent connection. Blazor WebAssembly runs the .NET runtime in the browser as a WASM binary — offline-capable, larger initial download." },
      { q: "What is the Blazor component lifecycle?",
        a: "OnInitialized(Async) runs once on mount. OnParametersSet(Async) runs when parameters change. ShouldRender controls re-render. OnAfterRender(Async)(firstRender) fires after the DOM is updated. StateHasChanged() triggers a re-render manually from async callbacks." },
      { q: "How does two-way data binding work with @bind in Blazor?",
        a: "@bind='property' is syntactic sugar for value='@property' plus oninput/onchange='@(e => property = e.Value)'. @bind:event='oninput' changes the trigger event. @bind works on input, select, and textarea elements, and on child component parameters." },
      { q: "How does Blazor JavaScript interop work?",
        a: "Inject IJSRuntime. Call await JS.InvokeVoidAsync('window.alert', 'hello') to call JS from C#. In JS, DotNet.invokeMethodAsync('Assembly', 'StaticMethod', args) calls a [JSInvokable] static C# method. For instance methods, use DotNetObjectReference.Create(this)." },
      { q: "What are cascading parameters in Blazor?",
        a: "<CascadingValue Value='theme'> in an ancestor propagates theme to all descendant components that declare [CascadingParameter] ThemeConfig Theme. This avoids prop-drilling for cross-cutting concerns like themes, auth state, and localization." },
      { q: "How does Blazor routing work?",
        a: "@page '/users/{Id:int}' at the top of a component registers the component as a route. NavigationManager.NavigateTo('/home') navigates programmatically. The Router component in App.razor matches the URL to a component and renders it in the Found template." },
      { q: "How do you validate a form in Blazor?",
        a: "Wrap inputs in <EditForm Model='person' OnValidSubmit='HandleSubmit'>. Add <DataAnnotationsValidator /> to wire DataAnnotations rules and <ValidationSummary /> to display all errors. Per-field errors appear with <ValidationMessage For='() => person.Email'/>." },
      { q: "How do you manage shared state in a Blazor application?",
        a: "Register a Scoped service (per-circuit in Blazor Server, per-session in WASM) that holds state. Inject it into components. Call StateHasChanged() in components after the service mutates state. Use events or Action callbacks on the service to notify subscribing components." },
      { q: "What are RenderFragment and templated components in Blazor?",
        a: "RenderFragment is a delegate representing a chunk of Blazor markup. Expose a [Parameter] RenderFragment ChildContent and render it with @ChildContent in the template. Templated components accept RenderFragment<T> to give the caller control over rendering each item." },
      { q: "How do you make HTTP requests in Blazor WebAssembly?",
        a: "Register a typed HttpClient with builder.Services.AddHttpClient<WeatherClient>(c => c.BaseAddress = new Uri(builder.HostEnvironment.BaseAddress)). The browser's fetch API handles the request under the hood. Use GetFromJsonAsync<T>() and PostAsJsonAsync() from System.Net.Http.Json." },
    ],
  },

  {
    name: "Gin",
    subtitle: "10 Q&A on routing, middleware, binding, error handling, and testing",
    accentHex: "#00ACD7",
    pairs: [
      { q: "What is Gin and what performance advantages does it have?",
        a: "Gin is a Go HTTP framework built on HttpRouter, a radix-tree router that resolves routes in O(k) where k is the URL depth. It has zero memory allocation per request on the hot path and benchmarks at ~50,000 req/s, significantly faster than standard net/http mux." },
      { q: "How do you define route groups and apply group-scoped middleware?",
        a: "api := r.Group('/api', authMiddleware). Inside, api.GET('/users', handler) inherits authMiddleware. Nesting groups further: admin := api.Group('/admin', adminOnly). This scopes middleware without repeating it on every individual route." },
      { q: "How do you write custom Gin middleware?",
        a: "A middleware is a HandlerFunc: func MyMiddleware() gin.HandlerFunc { return func(c *gin.Context) { // before; c.Next(); // after } }. Call c.Abort() or c.AbortWithStatus(401) to stop the chain without calling Next." },
      { q: "How does JSON binding and validation work in Gin?",
        a: "Define a struct with json and binding tags: Name string `json:'name' binding:'required'`. Call c.ShouldBindJSON(&req). On validation failure ShouldBindJSON returns an error; c.JSON(400, gin.H{'error': err.Error()}) sends the response. Gin uses go-playground/validator." },
      { q: "How does Gin's Context (c *gin.Context) work?",
        a: "c wraps request and response. c.Param('id') reads path params, c.Query('q') reads query params, c.ShouldBindJSON reads body. c.JSON(200, obj) serializes and writes JSON. c.Set('user', u) and c.Get('user') pass values through the middleware chain." },
      { q: "How do you handle errors centrally in Gin?",
        a: "Append errors with c.Error(err). After all middleware, a final middleware reads c.Errors and formats a JSON response. Alternatively use c.AbortWithStatusJSON(500, gin.H{'error': err.Error()}) inside a recovery middleware registered with gin.Recovery()." },
      { q: "How do you serve static files and HTML templates in Gin?",
        a: "r.Static('/assets', './static') serves a directory. r.StaticFile('/favicon.ico', './favicon.ico') serves one file. Load templates with r.LoadHTMLGlob('templates/**/*') and render with c.HTML(200, 'index.tmpl', gin.H{'title': 'Home'})." },
      { q: "How do you configure TLS in a Gin server?",
        a: "Replace r.Run(':8080') with r.RunTLS(':443', 'cert.pem', 'key.pem'). Alternatively pass a pre-configured *tls.Config via r.RunListener(tls.NewListener(l, tlsConf)). Use autotls or certmagic packages for Let's Encrypt automation." },
      { q: "How do you use gin.Context.Set/Get to pass data between middleware and handlers?",
        a: "c.Set('userID', 42) stores a value in the context for the current request lifecycle. c.Get('userID') returns (interface{}, bool). Use c.MustGet('userID').(int) for typed access. This is the correct way to share auth data from middleware to handlers." },
      { q: "How do you write tests for Gin routes?",
        a: "Create a test recorder: w := httptest.NewRecorder(). Build a request: req, _ := http.NewRequest('GET', '/users/1', nil). Call router.ServeHTTP(w, req). Assert w.Code and w.Body.String(). No server port needed — tests run fully in-process." },
    ],
  },

  {
    name: "Echo",
    subtitle: "10 Q&A on routing, middleware, binding, JWT auth, and testing",
    accentHex: "#00ACD7",
    pairs: [
      { q: "What is Echo and how does it compare to Gin?",
        a: "Both use radix-tree routing. Echo provides built-in middleware (Logger, Recover, CORS, JWT), automatic TLS via Let's Encrypt, and HTTP/2 support out of the box. Its echo.Context API is arguably more ergonomic with typed header/cookie helpers." },
      { q: "How do you apply middleware globally vs to a route group in Echo?",
        a: "e.Use(middleware.Logger()) applies globally to all routes. g := e.Group('/admin', AdminMiddleware) applies to the group only. Individual routes: e.GET('/profile', handler, AuthMiddleware). Middleware functions have signature func(next echo.HandlerFunc) echo.HandlerFunc." },
      { q: "How does request binding and validation work in Echo?",
        a: "c.Bind(&req) reads JSON body, form values, or query params into a struct based on tags. Echo does not validate by default. Register a validator (e.Validator = &MyValidator{}) that implements Validate(i interface{}) error and call c.Validate(&req) explicitly." },
      { q: "How do Echo Groups help organize routes?",
        a: "g := e.Group('/api/v1') prefixes all routes in the group. Apply shared middleware once: g.Use(JWTMiddleware). Create sub-groups: admin := g.Group('/admin', AdminOnly). This mirrors Express.Router and NestJS Module organization patterns." },
      { q: "How does Echo's custom error handler work?",
        a: "Set e.HTTPErrorHandler = func(err error, c echo.Context). Check if err is *echo.HTTPError for status/message; otherwise default to 500. This centralizes error formatting, allowing you to return consistent JSON error shapes across the entire application." },
      { q: "What does echo.Context provide for reading request data?",
        a: "c.Param('id') reads path params, c.QueryParam('q') reads query strings, c.FormValue('field') reads form fields, c.Request().Header.Get('X-Api-Key') reads headers, and c.Get/Set passes values through middleware to handlers via the request context." },
      { q: "How do you implement JWT authentication in Echo?",
        a: "Register echojwt.WithConfig(echojwt.Config{SigningKey: []byte(secret)}) as middleware on a Group. It reads the Bearer token, validates the signature, and populates the jwt.Token in the context. Retrieve claims with c.Get('user').(*jwt.Token).Claims." },
      { q: "What are Echo's response helpers and what do they return?",
        a: "c.JSON(200, obj) marshals obj and sets Content-Type application/json. c.XML, c.String, c.HTML, c.File, c.Stream cover other content types. c.NoContent(204) sends an empty response. c.Redirect(301, url) sets Location header and status." },
      { q: "How do you handle WebSocket connections in Echo?",
        a: "Use github.com/gorilla/websocket inside an Echo handler. Register a standard echo.HandlerFunc, upgrade the connection with upgrader.Upgrade(c.Response(), c.Request(), nil), then loop on conn.ReadMessage() and conn.WriteMessage() for full-duplex communication." },
      { q: "How do you test Echo handlers without starting an HTTP server?",
        a: "req := httptest.NewRequest(http.MethodGet, '/users/1', nil); rec := httptest.NewRecorder(); c := e.NewContext(req, rec); handler(c). Access rec.Code and rec.Body.String() for assertions. No listening port is required." },
    ],
  },

  {
    name: "Fiber",
    subtitle: "10 Q&A on Fasthttp, routing, middleware, context, and testing",
    accentHex: "#00ACD7",
    pairs: [
      { q: "What is Fiber and why is it built on Fasthttp instead of net/http?",
        a: "Fasthttp reuses request/response objects from a pool and avoids allocations. It is ~10x faster than net/http for raw throughput. Fiber wraps Fasthttp with an Express-inspired API, trading standard library compatibility for maximum performance." },
      { q: "How do you define parameterized routes in Fiber?",
        a: "app.Get('/users/:id', handler) captures :id as c.Params('id'). Wildcards: /files/*filepath. Optional: /users/:id?. Fiber's routing is case-insensitive by default. Grouped routes: api := app.Group('/api') — api.Get('/users', handler)." },
      { q: "How do you write and chain middleware in Fiber?",
        a: "A middleware returns nothing and uses c.Next() to pass control: app.Use(func(c *fiber.Ctx) error { fmt.Println('before'); err := c.Next(); fmt.Println('after'); return err }). app.Use('/api', authMiddleware) scopes to a prefix." },
      { q: "What does the Fiber Ctx object provide?",
        a: "c.Params('id'), c.Query('q'), c.Body() (raw bytes), c.BodyParser(&req) (JSON/form decode), c.Locals('user', val) / c.Locals('user') to pass data, c.JSON(data) to respond with JSON, c.Status(400).SendString('Bad') for explicit status + body." },
      { q: "How does Fiber parse and validate request bodies?",
        a: "c.BodyParser(&payload) deserializes the request body into a struct based on Content-Type (JSON, XML, form). Fiber uses encoding/json internally. For validation, pair BodyParser with go-playground/validator.v10 and call validate.Struct(payload)." },
      { q: "What are the tradeoffs of using Fasthttp (and thus Fiber)?",
        a: "Fasthttp does not implement net/http interfaces, so standard net/http middleware (Negroni, Alice) and testing helpers (httptest.NewRecorder) are incompatible. Use app.Test() for testing. Libraries expecting *http.Request cannot be used directly." },
      { q: "How do you implement JWT authentication middleware in Fiber?",
        a: "Use gofiber/contrib/jwt. Register jwtware.New(jwtware.Config{SigningKey: []byte(secret)}) on a Group. It validates the Bearer token and stores the parsed jwt.Token in c.Locals('user'). Retrieve claims with c.Locals('user').(*jwt.Token).Claims." },
      { q: "How do you connect Fiber to PostgreSQL?",
        a: "Use jackc/pgx or gorm with a PostgreSQL driver. Create a pgxpool.Pool or gorm.DB at startup and store it in the Fiber App Locals: app.Use(func(c *fiber.Ctx) error { c.Locals('db', pool); return c.Next() }). Handlers retrieve it with c.Locals('db')." },
      { q: "How do you test Fiber handlers?",
        a: "Use app.Test(req, timeout). Build a standard *http.Request: req, _ := http.NewRequest('GET', '/users/1', nil). Fiber processes it in-process and returns a *http.Response. Assert resp.StatusCode and read body with ioutil.ReadAll(resp.Body)." },
      { q: "What is Fiber's Recover middleware and why is it important?",
        a: "Recover catches panics in handlers and middleware, logs the stack trace, and returns a 500 response instead of crashing the goroutine. Without it, a panic in a handler would terminate the goroutine and could leak connections. Register it first with app.Use(recover.New())." },
    ],
  },

  {
    name: "Chi",
    subtitle: "10 Q&A on lightweight routing, middleware, context, and net/http",
    accentHex: "#00ACD7",
    pairs: [
      { q: "What is Chi and why is it described as idiomatic Go?",
        a: "Chi is a lightweight router that implements http.Handler and uses only standard net/http — no custom context types. Middleware is standard func(http.Handler) http.Handler. Any existing net/http handler works out of the box without adaptation." },
      { q: "How does Chi middleware chaining work?",
        a: "Chi uses alice-style functional composition. r.Use(middleware.Logger) applies globally. r.Group(func(r chi.Router) { r.Use(AuthMiddleware); r.Get('/admin', handler) }) scopes middleware to the group. Each middleware wraps the next http.Handler." },
      { q: "How do you read URL parameters in Chi?",
        a: "chi.URLParam(r, 'id') reads the named parameter from the URL. In a handler: id := chi.URLParam(r, 'id'). Chi stores params in the request context under its own key, making them accessible without a custom Context type via the standard library." },
      { q: "What is chi.Mount() and how do you use it?",
        a: "r.Mount('/api', apiRouter) attaches a sub-router at a prefix. apiRouter is itself a *chi.Mux. Mount strips the prefix before passing the request to the sub-router. This enables fully independent route trees for modular codebases." },
      { q: "How do you pass values between middleware and handlers in Chi?",
        a: "Use standard context: ctx := context.WithValue(r.Context(), userKey{}, user); r = r.WithContext(ctx). Retrieve in the handler: user := r.Context().Value(userKey{}).(User). Chi recommends unexported struct keys to avoid collisions with other packages." },
      { q: "How do you implement JWT authentication middleware in Chi?",
        a: "Write a func(next http.Handler) http.Handler that reads the Authorization header, validates the JWT, and on success calls next.ServeHTTP(w, r.WithContext(ctx)) with the user in the context. On failure, write 401 and return without calling next." },
      { q: "What built-in middleware does Chi provide?",
        a: "middleware.Logger (structured request logging), middleware.Recoverer (panic recovery), middleware.Throttle(N) (concurrent request limiter), middleware.Compress (gzip), middleware.StripSlashes, middleware.Heartbeat('/health'), and middleware.RealIP." },
      { q: "How do you define RESTful resource routes in Chi?",
        a: "r.Get('/users', listUsers); r.Post('/users', createUser); r.Route('/users/{id}', func(r chi.Router) { r.Get('/', getUser); r.Put('/', updateUser); r.Delete('/', deleteUser) }). Chi does not have a resources() shortcut — explicit routes are idiomatic." },
      { q: "How does Chi handle 404 and 405 responses?",
        a: "r.NotFound(handler) sets a custom 404 handler for unmatched routes. r.MethodNotAllowed(handler) sets a 405 handler when a route exists but not for the used HTTP method. Without custom handlers, Chi returns plain text 404/405 by default." },
      { q: "How do you write tests for Chi handlers?",
        a: "Chi handlers are plain http.HandlerFunc, so use httptest.NewRequest and httptest.NewRecorder directly. Set chi URL params with chitest: rctx := chi.NewRouteContext(); rctx.URLParams.Add('id', '42'); r = r.WithContext(context.WithValue(r.Context(), chi.RouteCtxKey, rctx))." },
    ],
  },

  {
    name: "Laravel",
    subtitle: "10 Q&A on MVC, Eloquent, queues, service container, and testing",
    accentHex: "#FF2D20",
    pairs: [
      { q: "What is Laravel's request lifecycle from incoming HTTP to response?",
        a: "HTTP request enters public/index.php, bootstraps the service container, passes through HTTP Kernel middleware (CORS, session, auth), dispatches to the Router, which resolves the controller method and returns a Response that Laravel sends back." },
      { q: "How does Laravel Eloquent ORM define model relationships?",
        a: "Define a public method in the model. hasMany(Order::class) returns a HasMany relation (posts for a user). belongsTo(User::class) defines the inverse. Eager-load with User::with('orders')->find(1) to avoid N+1. ManyToMany uses a pivot table via belongsToMany." },
      { q: "How does Laravel's service container resolve dependencies?",
        a: "When Laravel constructs a controller, it reads its constructor type hints and resolves each from the container. Bind an interface: $this->app->bind(PaymentGateway::class, StripeGateway::class). All classes are resolved recursively through the container." },
      { q: "What is Laravel Artisan and what are five common commands?",
        a: "php artisan make:model Order -m (model + migration), make:controller (controller), migrate (apply migrations), queue:work (start queue worker), route:list (print all routes), tinker (REPL against the app). Custom commands extend Illuminate\\Console\\Command." },
      { q: "How does Laravel handle queued jobs?",
        a: "Implement ShouldQueue on a Job class. Dispatch with SendEmail::dispatch($user). A queue worker (php artisan queue:work) fetches jobs from the backend (Redis, SQS, DB) and executes them. Failed jobs are stored in failed_jobs and can be retried with queue:retry all." },
      { q: "What is Laravel Sanctum and how does it authenticate SPAs?",
        a: "Sanctum issues API tokens stored in the personal_access_tokens table. For SPAs it uses cookie-based session auth with CSRF protection via the /sanctum/csrf-cookie endpoint. Token-based auth is for mobile clients. Both use the Auth::user() facade." },
      { q: "How does Laravel Blade template inheritance work?",
        a: "@extends('layouts.app') references the parent template. @section('content') and @endsection define overrideable regions. @yield('content') in the parent marks where child content renders. @include('partials.nav') embeds a partial. @stack/@push build asset stacks." },
      { q: "How do you write feature tests in Laravel?",
        a: "Subclass Tests\\TestCase and use the RefreshDatabase trait to reset DB per test. $response = $this->postJson('/api/login', ['email'=>..., 'password'=>...]). Assert $response->assertStatus(200)->assertJsonStructure(['token']). Use Sanctum::actingAs(user) for auth." },
      { q: "What are Laravel Policies and how do they control authorization?",
        a: "A Policy class has methods like view(User $user, Post $post). Register in AuthServiceProvider. Call $this->authorize('view', $post) in a controller — Laravel resolves the policy and throws 403 on failure. Gate::allows('view', $post) checks without throwing." },
      { q: "What is Laravel Livewire and how does it work?",
        a: "Livewire is a full-stack component framework. A PHP class (extends LivewireComponent) holds state and methods. The Blade template uses wire:model for two-way binding and wire:click for actions. AJAX requests sync state between browser and server on each interaction." },
    ],
  },

  {
    name: "Symfony",
    subtitle: "10 Q&A on DI, routing, Doctrine, events, security, and testing",
    accentHex: "#000000",
    pairs: [
      { q: "What is Symfony Flex and how does it manage bundles?",
        a: "Symfony Flex is a Composer plugin that maps package aliases (symfony/orm-pack) to recipes stored on a Flex server. On install it runs the recipe: creates config files, registers the bundle in bundles.php, and adds env vars to .env. This automates all boilerplate." },
      { q: "How does Symfony's DI container and autowiring work?",
        a: "Symfony reads services.yaml and scans src/ for classes to register as services. Autowiring reads constructor type hints and injects matching services automatically. Interface bindings ($logger: Psr\\Log\\LoggerInterface) resolve abstractions to concrete implementations." },
      { q: "How does Symfony's routing system work?",
        a: "#[Route('/users/{id}', name: 'user_show', methods: ['GET'])] on a controller method registers the route via PHP 8 attributes. Alternately use config/routes.yaml. Route::generate('user_show', ['id'=>1]) produces the URL. Prefix routes with #[Route('/api')] on the class." },
      { q: "What is Doctrine ORM and how does it differ from Active Record?",
        a: "Doctrine uses the Data Mapper pattern: entities are plain PHP objects with no database knowledge. An EntityManager (separate object) handles persistence. Active Record (e.g., Eloquent) embeds DB logic in the model itself. Data Mapper enables richer domain models." },
      { q: "How does the Symfony EventDispatcher work?",
        a: "Dispatch an event: $dispatcher->dispatch(new OrderCreated($order)). A subscriber implements EventSubscriberInterface and returns getSubscribedEvents(): [OrderCreated::class => 'onOrderCreated']. Tag the subscriber with kernel.event_subscriber for auto-registration." },
      { q: "How does Symfony Security's firewall system work?",
        a: "A firewall in security.yaml defines which URLs it protects and how (form_login, jwt, api_key). The security layer authenticates the request and populates a token in the TokenStorage. Access control rules (access_control) check roles on top of the authenticated token." },
      { q: "How do Symfony Forms handle validation?",
        a: "A FormType defines fields and maps them to a model class. $form->handleRequest($request) binds submitted data. $form->isSubmitted() && $form->isValid() checks Data Annotation (or YAML) constraints. $form->getErrors(true) returns validation messages." },
      { q: "How do you create a Symfony Console command?",
        a: "Extend Command. Set the name in configure() via $this->setName('app:greet'). Define input arguments/options with addArgument/addOption. Implement execute($input, $output). Run with php bin/console app:greet --name=Alice. Tag with console.command for auto-discovery." },
      { q: "How does Symfony Cache work with PSR-6?",
        a: "Symfony's CacheInterface (a superset of PSR-6) provides get($key, callable). Use cache.adapter.redis in config to back the pool with Redis. @Cache(expires='1 hour') on a controller action caches the HTTP response via the HttpCache reverse proxy." },
      { q: "How do you write functional tests in Symfony?",
        a: "Extend WebTestCase. $client = static::createClient(); $crawler = $client->request('GET', '/users/1'); $this->assertResponseIsSuccessful(). Use $client->clickLink() and $client->submitForm() to simulate user interaction. KernelTestCase is used for service-layer tests." },
    ],
  },

  {
    name: "CodeIgniter",
    subtitle: "10 Q&A on MVC, routing, Query Builder, validation, and testing",
    accentHex: "#DD4814",
    pairs: [
      { q: "What is CodeIgniter and how does it implement MVC?",
        a: "CodeIgniter 4 is a lightweight PHP framework. Controllers in app/Controllers extend BaseController and handle HTTP logic. Models in app/Models extend Model and interact with the database. Views in app/Views are PHP files rendered with return view('name', data)." },
      { q: "How does CodeIgniter 4 routing work?",
        a: "Routes are defined in app/Config/Routes.php: $routes->get('users/(:num)', 'UserController::show/$1'). $routes->resource('users') creates CRUD routes automatically. Auto-routing (deprecated) mapped URLs to controllers by convention." },
      { q: "How does the CodeIgniter Query Builder prevent SQL injection?",
        a: "Query Builder methods accept raw values that are automatically escaped and parameterized. $db->where('id', $id)->get('users') generates a prepared statement. Never concatenate user input into query strings; use Query Builder or $db->query('SELECT * FROM u WHERE id=?', [$id])." },
      { q: "What are CodeIgniter 4 Models and Entity classes?",
        a: "A Model extending CI4's Model gets CRUD helpers (find, insert, update, delete) and built-in validation. An Entity class maps a database row to a typed PHP object with casting (int, bool, datetime). Set protected $returnType = UserEntity::class in the model." },
      { q: "How does CodeIgniter validation work?",
        a: "Use $this->validate($rules) in a controller, where $rules is an array like ['email' => 'required|valid_email']. Define reusable rule sets in app/Config/Validation.php. $this->validator->getErrors() returns field-level error messages after a failed validation." },
      { q: "How does CodeIgniter 4 handle sessions differently from PHP native sessions?",
        a: "CI4's session library wraps native sessions but adds configurable drivers (files, database, Redis, Memcached). Sessions are signed with an encryption key to prevent tampering. set_flashdata('key', val) stores one-request messages. Regeneration is automated." },
      { q: "How do you handle file uploads in CodeIgniter 4?",
        a: "Access via $file = $this->request->getFile('avatar'). Call $file->isValid() and $file->hasMoved() before moving. $file->move(WRITEPATH.'uploads', $file->getRandomName()) stores it. Validate MIME type with getClientMimeType() and check file size against maxSize." },
      { q: "What is a CodeIgniter RESTful Resource Controller?",
        a: "php artisan make:controller UserController --rest generates index, show, create, store, edit, update, delete stubs. $routes->resource('users', ['controller'=>'UserController']) maps HTTP verbs to these methods. Override only the methods your resource needs." },
      { q: "How does CodeIgniter 4 Filters work?",
        a: "Filters (like middleware) run before or after controllers. Define a filter class with before() and after() methods. Register in app/Config/Filters.php under $aliases. Apply globally or per-route: $routes->get('admin/.*', 'AdminController', ['filter' => 'auth'])." },
      { q: "How do you test a CodeIgniter 4 controller?",
        a: "Extend CIUnitTestCase. Use $result = $this->withUri('http://example.com/users/1')->controller(UserController::class)->execute('show', 1). $result->assertStatus(200) and $result->assertSee('Alice') check the response without starting an HTTP server." },
    ],
  },

  {
    name: "Ruby on Rails",
    subtitle: "10 Q&A on MVC, Active Record, routes, jobs, and testing",
    accentHex: "#CC0000",
    pairs: [
      { q: "What does 'Convention over Configuration' mean in Rails?",
        a: "Rails derives behavior from naming conventions. A User model maps to the users table. UserController handles /users routes. This means less config: no mapping files are needed. Follow the convention and Rails wires everything automatically; deviate and you configure explicitly." },
      { q: "What is the Rails request lifecycle?",
        a: "A request hits the Router (config/routes.rb) which dispatches to a controller action. The action interacts with models, then renders a view or returns JSON. Rack middleware (CSRF, session, logger) wraps the entire stack and runs before/after the router." },
      { q: "What is Active Record and how do associations work?",
        a: "Active Record implements the active record pattern: model instances map to rows and class methods generate SQL. has_many :orders in User and belongs_to :user in Order define the association. user.orders loads associated rows. user.orders.create(total:100) inserts and associates." },
      { q: "How do Rails migrations work and how do you roll one back?",
        a: "rails generate migration AddAgeToUsers age:integer creates a timestamped migration file. rails db:migrate runs all pending Up methods. rails db:rollback runs the most recent Down method, reversing the change. db:migrate VERSION=20230101 migrates to a specific version." },
      { q: "How does Rails routing work with resources?",
        a: "resources :users in routes.rb generates seven RESTful routes (index, show, new, create, edit, update, destroy). namespace :api {resources :users} prefixes routes and expects Api::UsersController. match '/about', via: :get defines a custom route." },
      { q: "What are Rails concerns and what problems do they solve?",
        a: "A concern is a module in app/models/concerns or app/controllers/concerns using ActiveSupport::Concern. Include them in models/controllers to share reusable behavior (Taggable, Auditable) without multiple inheritance. included do ... end runs in the including class's context." },
      { q: "How does Active Job work with Sidekiq?",
        a: "Define a job class extending ApplicationJob with a perform(*args) method. Set queue_adapter :sidekiq in ApplicationJob. Enqueue with MyJob.perform_later(user_id). Sidekiq workers pick up jobs from Redis, call perform, and handle retries on failure with exponential backoff." },
      { q: "What is Action Cable in Rails?",
        a: "Action Cable integrates WebSockets into Rails using channels. A channel class (ApplicationCable::Channel) defines subscribed and receive methods. The client subscribes via App.cable.subscriptions.create('ChatChannel', callbacks). Broadcast from server with ActionCable.server.broadcast." },
      { q: "How does Rails asset management work with Propshaft?",
        a: "Propshaft (Rails 7+) is a simpler asset pipeline. It fingerprints files for cache busting and serves them from /assets. Unlike Sprockets, it does not transpile or concatenate — use importmap-rails for ES modules or a bundler (esbuild, Vite) separately for complex JS." },
      { q: "How do you test a Rails model with RSpec and FactoryBot?",
        a: "FactoryBot.define { factory :user { name 'Alice'; email 'alice@ex.com' } }. In the spec: let(:user) { create(:user) }. Test associations, validations, and methods with expect(user.email).to eq('alice@ex.com') and expect(user).to be_valid. Run with bundle exec rspec." },
    ],
  },

  {
    name: "Sinatra",
    subtitle: "10 Q&A on routes, middleware, templates, helpers, and testing",
    accentHex: "#CC0000",
    pairs: [
      { q: "What is Sinatra and how does it differ from Ruby on Rails?",
        a: "Sinatra is a DSL for building web apps in Ruby with minimal structure. There is no MVC, no ORM, no asset pipeline by default. You define routes with get/post/put/delete and return a string or render a template. Rails is a full framework; Sinatra is a micro-framework." },
      { q: "How do you define and read URL parameters in Sinatra?",
        a: "get '/users/:id' do; user_id = params[:id]; end. Wildcard: get '/files/*' do; path = params['splat'].first; end. Query strings are also in params: GET /search?q=ruby gives params[:q] == 'ruby'. Route blocks have access to request, response, and session." },
      { q: "How does Sinatra handle request data (body, headers, forms)?",
        a: "request.body.read returns the raw request body. request.env['HTTP_AUTHORIZATION'] reads a header. params includes both query string and form fields from application/x-www-form-urlencoded or multipart. For JSON: JSON.parse(request.body.read)." },
      { q: "What template engines does Sinatra support and how do you render them?",
        a: "Sinatra supports ERB (erb :view), Haml (haml :view), Slim, Mustache, and others. Install the corresponding gem, then call the helper with a symbol matching the file name in views/. Pass local variables: erb :user, locals: { user: @user }." },
      { q: "How do Sinatra before/after filters work?",
        a: "before do; @user = User.find(session[:user_id]); end runs before every route. after do; log_response(response); end runs after. Filters accept a pattern: before '/admin/*' do; halt 401 unless @user&.admin?; end scopes to matching paths." },
      { q: "How do you define and use helper methods in Sinatra?",
        a: "Helpers inside a helpers do block or a module included via helpers MyHelpers become available in both route blocks and templates. def link_to(text, href); \"<a href='#{h(href)}'>#{h(text)}</a>\"; end. h() is the built-in HTML escape helper." },
      { q: "How do you handle errors and define custom error pages?",
        a: "not_found do; 'Page not found'; end handles 404. error 500 do; 'Server error'; end handles 500. error MyCustomException do; status 400; 'Bad input'; end catches a specific exception class. error do catches any unhandled exception." },
      { q: "What is the difference between classic-style and modular Sinatra?",
        a: "Classic style (require 'sinatra') defines routes at the top level — simple but pollutes the global namespace. Modular style (class MyApp < Sinatra::Base) is a reusable class you mount in a Rack config.ru. Modular is preferred for tests and embedding in larger apps." },
      { q: "How do you manage sessions and cookies in Sinatra?",
        a: "Enable sessions with enable :sessions (uses Rack::Session::Cookie with a random secret by default). Set a secret with set :session_secret, 'long-random-string'. session[:user_id] = user.id stores data. Cookies: response.set_cookie('name', value: 'x', httponly: true)." },
      { q: "How do you test a Sinatra application with Rack::Test?",
        a: "Include Rack::Test::Methods and define app { Sinatra::Application } (or MyApp). Call get '/path', headers: {'Authorization'=>'Bearer token'}. Assert last_response.status == 200 and last_response.body. set :environment, :test to suppress output." },
    ],
  },
];
